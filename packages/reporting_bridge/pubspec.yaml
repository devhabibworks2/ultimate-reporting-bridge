name: reporting_bridge
description: Typed host–presenter bridge messages for Ultimate Report Builder (see bridge contract documentation).
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  archive: ^4.0.7

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.25.0
